# Findings

Research spike outputs, one file per question: `<slug>.md`. Written by `plumb-research` /
the `researcher` agent. Every number carries a cited, dated source.

A finding is not a decision. When a finding resolves an open decision it produces a draft ADR in
`../decisions/`, which Amine signs before it moves into `specs/02-architecture-technique.md` §2.
