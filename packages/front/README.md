# @plumb/front — production UI

Empty, and **blocked by decision D-01** (Tauri vs SwiftUI vs Next.js). Nothing here is written
before that is an ADR.

What can be prepared meanwhile, platform-agnostic:
- the design-token layer generated from `specs/06-design-system-ui.md` §2,
- the French label index from `specs/06-design-system-ui.md` §11,
- the WebSocket event-handling logic (the five event types from the contracts).

Skill: `plumb-ui`. Agent: `frontend-engineer`.
