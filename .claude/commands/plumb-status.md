---
description: Where the Plumb project stands and what to work on next
---

Report the state of Plumb, concisely:

1. Read `CLAUDE.md` (state + open decisions), `docs/ai-harness/tasks/` and
   `docs/ai-harness/workflow.md`.
2. Check what actually exists on disk under `packages/` — trust the filesystem, not the docs.
3. Answer in this shape:
   - **Built:** what exists and passed review.
   - **In flight:** what is started and not reviewed.
   - **Next unblocked task:** one task, with its brief path and the skill that handles it.
   - **Blocked:** what is waiting, and on which decision.
   - **Highest-leverage decision:** the one call from Amine that unblocks the most work.

If the docs and the filesystem disagree, say so — that drift is the finding.
