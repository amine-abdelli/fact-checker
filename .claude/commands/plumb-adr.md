---
description: Record an architecture decision for Plumb
argument-hint: [the decision, e.g. "use Tauri for the MVP desktop app"]
---

Record this decision: $ARGUMENTS

1. Read `specs/02-architecture-technique.md` §2 to get the next ADR number and match the existing
   format exactly (ADR-01 … ADR-08 are the model).
2. Check `docs/ai-harness/findings/` for the finding that supports it, and cite it.
3. Write the ADR: context, decision, alternatives considered, consequences, status and date.
4. Save it to `docs/ai-harness/decisions/ADR-<nn>-<slug>.md` and show it to me for sign-off.
   **Do not edit `specs/02-architecture-technique.md` yourself** — I move the ADR into the spec once I have signed it.
5. Then list what the decision unblocks: which tasks, which skills, which files.

If the decision contradicts an existing ADR, say which one and stop.
